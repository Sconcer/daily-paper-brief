# Third-party notices

## humanizer-zh

The bundled snapshot identifies itself as a Chinese adaptation of
`blader/humanizer`, with reference to `hardikpandya/stop-slop`. Its original MIT
`LICENSE` and attribution-bearing `README.md` are preserved unchanged under
`skills/humanizer-zh/`.

The repository-wide Apache-2.0 license does not replace that MIT license.

## arXiv-sourced material

The repository contains retrieval and transformation code, not a license grant
for papers. Metadata, PDFs, figures, source archives, linked code, and datasets
can have different terms. Runtime outputs are ignored by Git and require a
separate rights review before redistribution.
